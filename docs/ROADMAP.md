# Roadmap

Deferred ≠ declined (§12). Anything here may land once it passes three gates:
it fits the §04 vault contract (or bumps the major version with a documented
migration), it fits the §06 budgets or ships lazily, and it obeys §02. Process
is roadmap entry → ADR → milestone.

Three kinds of entry live here. The **§12 table** below is the designed expansion
path — features deliberately left out of v1, each with the landing path that
keeps it contract-safe. After it come risks and decisions **parked during a
phase**, recorded here rather than in a commit message nobody will re-read. Last
are things **decided after release**, raised by using the thing rather than by
building it: those are answers, not deferrals, and they are written down so the
same question is not re-argued with every new pair of eyes.

Every entry names a trigger — for a parked item, what would un-park it; for a
decided one, what would reopen it. An entry without one is a wish.

## Post-v1 expansion (§12, seeded by P11)

| Deferred feature | Designed landing path (contract-safe) | Prerequisite / trigger |
|---|---|---|
| **Query & table views ("databases")** | Inline queries over frontmatter + tags, evaluated client-side and rendered as §02 tables — the Dataview pattern. Notes stay plain files; a query is just text in a note. | Query-grammar ADR; v1 corpus/store stable. |
| **Kanban / boards** | A view over existing tasks + tags, columns keyed by tag or status. Pure derivation, zero new storage. | The query engine above. |
| **Graph view** | Pure derivation of the backlinks map into a monochrome, hairline-styled panel. No new data — `core/links.ts` already computes it. | Demand-driven; lazy chunk within budget. |
| **Plugin system** | Versioned, capability-scoped extension points (commands, panels, decorations, query functions) once the core API freezes at v1.x. A plugin may never bypass `vault.rs` writes. | API freeze + a security-model ADR. |
| **Richer editing surface** | ~~Images shipped in §04/§02b Rev O~~ — drawn *under* their own `![alt](src)` rather than in place of it, which keeps this row's own "markdown stays the literal source" and avoids answering the open §02 question about folding by accident. Tables and math remain, and both are the harder half: each would replace the source rather than annotate it. | For the rest: a §02 ruling on **replacing** the source, which is a different question from the one frontmatter folding just answered. §12 permits hiding "provided it still passes §02", and a fold passes because it is reversible in place and the bytes never move. A rendered table is not that — the source is gone while you look at the result, and there is nowhere to put the caret to get it back. |
| **Realtime co-editing** | Optional CRDT transport layered on top (reopening ADR-001). Files stay canonical; every session flushes to plain markdown. | True multi-writer demand. |
| **Built-in richer sync** | From git checkpoints (P12) toward managed git remotes or an optional relay — always self-hostable, never required. | P12 shipped; user pull. |
| **Importers** | One-way converters (Obsidian, Notion export, Logseq) into the §04 format. Read-only, low risk, high adoption value. | None — the best first post-v1 milestone. |
| **Native shells / mobile ergonomics** | A Tauri wrapper around the same embedded UI. Remote/tailnet mode already serves every device meanwhile. | PWA ergonomics measured insufficient. |
| **E2E-encrypted remote** | Token mode (P12) hardened with encrypted transport, for vaults hosted off-device. | Remote mode adoption. |

## Parked during P2 (server core)

| Item | Why it is parked | Trigger |
|---|---|---|
| **Etag collision window** | §04 mandates `mtime + len`, so two different bodies of identical length written inside one filesystem mtime tick share an etag. Measured sub-microsecond on macOS/APFS, so it is not reachable in practice there; ext4's granularity is coarser. Changing the etag scheme would change §04. | A real collision, or a §04 revision. The client rule meanwhile: an equal etag on a `changed` frame must **not** be read as "no change". |
| **~~Cross-process write races~~** | ~~Closed.~~ `serve` claims the vault before it binds and refuses when somebody else holds it, so the in-process lock is once again describing the truth. Three decisions are worth recording. The claim lives in the **temp directory**, not `.register/`: one untracked file there makes a vault under git permanently dirty for as long as the app runs — `?1` in the status bar, and the test that exists to say the app never touches your repository going red — and `.gitignore` cannot fix it, because adding a third line to the scaffold is a §04 surface change under hard rule 1 and would reach no vault anybody had already initialised. The cost of that, stated because it is real: two processes not sharing a temp directory cannot see each other's claim, and a container and its host mounting one vault is exactly that case. The mechanism is `create_new`, one filesystem operation that both creates and refuses, rather than `fs4` or `fd-lock` — real advisory locking, an ADR under hard rule 6, and the wrong shape anyway, since what is wanted is a refusal at startup and not a lock held across every write. And a claim whose pid is **gone is taken over rather than obeyed**, which the first version did not do: a process killed outright never reaches `Drop`, so without that one `docker kill` left a vault unstartable until a human deleted a file — a worse failure than the race, and a new one. Liveness is `kill -0` in a subprocess, since std exposes no `kill`; every way of not knowing answers "still held", because a stranded lock is an inconvenience with a printed fix and a stolen one is the thing this prevents. Graceful shutdown releases it: `with_graceful_shutdown` on SIGTERM and ctrl-c, which is what `docker stop` sends, and which finally uses the `signal` feature that had been enabled in `Cargo.toml` and called nowhere. | — |
| **Any loopback origin is trusted** | The origin guard allows `http://localhost:<any port>` so `pnpm dev` can proxy from vite. That grants the same authority to every other local web server. Narrowing it needs an explicit dev-origin flag. | P12, alongside token mode — the same conversation about who may talk to the server. |
| **~~No WebSocket keepalive~~** | ~~Closed.~~ `pump()` pings an idle socket every 30 s and bounds every send at 10 s. Both halves were needed and for different reasons: without the ping a connection that has died — a laptop closed on a tailnet, a NAT entry dropped — is indistinguishable from a quiet vault, so the socket stays open, the client believes it is live, and the next agent edit is delivered to nobody. Without the timeout a half-open socket takes writes into the kernel buffer until it fills and then blocks for good, holding the task and its broadcast slot for the life of the process. The cadence is a named field rather than a constant, `Checkpointer::with_idle`'s trick, so the test proves it in 60 ms instead of half a minute — and the test was checked against its own absence: remove the ping arm and it reports "no frame inside two seconds". | — |
| **`If-Match: *`** | RFC 9110 defines the wildcard as "matches iff the resource exists". Unimplemented; no client sends it. | A second client, or a spec revision that asks for it. |
| **Loopback is read as "the owner", and on a shared host it is not** | `decide()` exempts any loopback peer, so on a multi-user machine every other uid gets full read/write/delete over 127.0.0.1 — including on a vault whose mode 700 is keeping them out at the filesystem level. The signal that would settle it is a peer credential, and TCP does not carry one portably; `SO_PEERCRED`/`getpeereid` want a Unix socket, which is a §04 API-surface change rather than a patch. Documented in SECURITY.md meanwhile. | Anyone running this on a shared host — or a §04 revision that adds a socket transport. |
| **The released binaries are unsigned** | A tag now publishes `SHA256SUMS` alongside the five assets, which catches a corrupted or substituted download but proves nothing about who built it. Real provenance wants either cosign keyless signing or GitHub artifact attestations, and both add a workflow permission and a verification story that has to be documented or it is theatre. | The first release anyone but the author installs. |
| **~~`the_servers_own_atomic_write_reports_one_change_not_a_temp_file` is flaky~~** | ~~Closed.~~ Not a race with the coalescer and not really flaky: a **release** build failed it 24 times out of 24, which is what made it diagnosable. The fixture seeds its note before `Watch::start`, and macOS delivers those events to the stream anyway — the opposite of what the `ARM` comment claimed — so `drain` collected the seed's event alongside the write's and the assertion read as a debouncer leak. Arming now empties the channel rather than only waiting on it: 0 failures in 30 release runs and 15 debug runs, and removing the drain brings it back 5 times in 6. Harmless while the repository is one person's, and corrosive once it is public: a red run a contributor did not cause is how people learn to re-run CI without reading it. | The first time it fails a pull request that is otherwise green — or sooner, since the repository is now public. |
| **~~`cargo-audit` in CI~~** | ~~Closed after P11.~~ The trigger was "P10/P11 release engineering, where CI grows anyway", and it did — `ci.yml` now has an `audit` job. It is the check that would have found RUSTSEC-2025-0068 without ADR-001 having to find it by hand. | — |

## Parked during P3 (client store)

Three P3 entries are now closed by SPEC Rev F: refs are never reissued, the
status bar's VAULT field is served, and `/api/tree` and event frames are
validated at the boundary instead of cast. What remains:

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~A conflict copy is an ordinary note~~** | ~~Closed by P13.~~ Screen 4 is built. A copy now reads as an artefact in the index — the filename rather than the original's title, `—` for a ref it does not own, and `UNRESOLVED` where a note shows its word count — and clicking it opens the merge rather than the editor. The announcement is derived from the tree instead of remembered from the moment: a latched `UNRESOLVED n` field beside FILES and GIT, which the next save cannot clear. That also closes a hole nobody had named — `#writeConflictCopy` only ever set the notice `if (this.openPath === path)`, so navigating during the two round trips parked a copy and told you nothing at all. | — |
| **~~GIT is permanently dashed~~** | ~~Closed by P12~~ — see the P12 section below, which carries the same entry closed. | — |
| **Cross-tab ref collision** | The server now allocates `nextRef`, so a single client cannot reissue a ref. Two tabs that fetch the tree at the same moment still both receive the same `nextRef`, and because they usually pick different slugs the paths differ — so `create()`'s free-name check passes for both and two notes end up sharing a ref. Closing it needs the server to allocate atomically at write time, which §04's path-addressed PUT cannot express. | Multi-tab or multi-agent creation proving it out; would want a create-if-absent addition to §04. |

## Parked during P6 (search, backlinks, tags)

| Item | Why it is parked | Trigger |
|---|---|---|
| **A tag is not clickable** | The sidebar and inspector show tags with counts and meters, but nothing happens when you click one. Filtering the index by tag — or seeding ⌘K with it — is the obvious next move, and §02b's state matrix defines no tag component, so building one would be inventing where §02b says "do not invent". | A §02b revision that draws the interaction. |
| **~~Conflict copies are unsearchable~~** | ~~Closed by P13, by answering rather than by changing it.~~ They are still out of the search index, the link graph and the tag counts, and that is still right — they carry the original's ref, title and tags and would double every one. What was actually wrong was reachability: "reachable only from the index pane" was the whole complaint, and there are now three routes in. The status bar names the count, `RESOLVE · CONFLICT` appears in ⌘K when there is one, and the index row says what it is. | A copy that is genuinely lost — which now needs all three to fail. |
| **~~The outline ignores setext headings~~** | ~~Closed.~~ `Title` over `=====` is a heading in CommonMark and does not appear in the OUTLINE pane — because the editor does not style it as one either, and the pane and the page have to agree. Both should change together or neither. | Anyone writing setext in a real vault. |
| **~~No guard on the chunk graph (P6)~~** | ~~Closed.~~ `app/e2e/chunks.spec.ts` reads what Vite emitted: the shell chunk must not statically import the editor chunk, and `index.html` must not `modulepreload` one — which would load it at boot just as surely without appearing in any import list. Confirmed to bite by reproducing the original regression: a static editor import in the shell moves `shell-*.js` from 15.34 kB gz to 15.73, against a 60 kB budget with 44 kB of headroom, so the size gate would never have noticed. It lives in the e2e job rather than in vitest for a mechanical reason — CI runs `pnpm test` *before* `pnpm build`, so the same assertion there would pass by finding nothing. | — |

## Parked during P7 (daily, tasks, templates)

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~⌘D is claimed twice~~** | ~~Closed at your instruction (Rev T), on §08 P7's side: ⌘D opens the daily log.~~ It was built to §02b Screen 2 because the wireframe is what a user reads off the screen — sound reasoning that answered the wrong question, since *which document a builder follows* is not *which key the product wants*. What settled it was arithmetic rather than authority: `G T` already opened TODAY, so ⌘D was a whole modifier spent on a second route to a view that had one, while the daily log — the note a person opens every morning — had only the chord. Screen 2's row draws `[G T]` now and DAILY LOG draws `[⌘D]`; both keys already existed, so nothing gained or lost a binding. `G D` stays bound though the palette no longer draws it: `G I` and `G T` are the rest of a family and dropping only this one would be an arbitrary hole in it. The three e2e tests that pressed ⌘D for TODAY press `G T`, and a new one pins the key, the created-on-demand log behind it, and both palette rows — checked against its own absence. | — |
| **"Today" is UTC, not local** | `daily/YYYY-MM-DD.md` is named from `toISOString()`, so west of Greenwich the evening's daily log is tomorrow's date. §02 already rules that "timestamps are UTC everywhere in the chrome — local time is never displayed", so making the day local is a doctrine change rather than a bug fix, and not mine to make. | A user outside UTC±0 keeping a daily log; would want §02 to say what a *date* is, as distinct from a timestamp. |
| **A ticked task vanishes with no undo** | §02b Screen 5 lists open tasks, so ticking one removes its row. The count still shows it in `total`, and the source note still holds it, but there is no way back from TODAY itself. | A second look at Screen 5; showing done tasks dimmed and struck through is the obvious alternative and the state matrix already describes that appearance. |
| **Templates are searchable but not actionable** | ~~Closed after P8.~~ A first real vault made the answer obvious: `core/paths.ts` now separates what the INDEX lists from what derivations count, so `templates/` and `CLAUDE.md` are out of the index and the tag meters but still findable in ⌘K. | — |

## Parked during P8 (init + agent contract)

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~The e2e stops at the frame, not the pixel~~** | ~~Closed by P11.~~ Playwright arrived with ADR-005 and `e2e/budgets.spec.ts` now measures the paint itself, with a MutationObserver rather than a poll. The Rust test still measures the frame at 48 ms; the browser measures what follows it. | — |
| **~~`register new` only works inside the vault~~** | ~~Closed.~~ `--vault DIR` names it instead. The default is unchanged and still §08 P8's specification — the flag exists because a `cd` inside a script outlives the command that needed it. A named directory takes the same vault check the current one always did, so a mistyped `--vault` is refused rather than scattering notes into it. |
| **Slug folding is partial, not NFKD** | The client normalises with `normalize('NFKD')`; Rust's std has no Unicode normalisation, so `scaffold::slug` folds the Latin-1 accented range by hand and lets everything else become a dash. `Café notes` matches on both sides; a Cyrillic or CJK title does not — it slugs to `untitled` in the CLI. The ref still identifies the note either way. | A vault in a non-Latin script, or a Unicode crate arriving for another reason. |
| **~~Two `register` processes can still race~~** | ~~Closed by the P2 entry on cross-process writes, which was always the same defect seen from a second angle:~~ `create`'s "is this name free" check sits outside the write lock, and one process cannot collide with itself. Nothing about `create` changed — the second process is refused before it reaches any of this. | — |

## Parked during P9 (BYOF + font themes)

| Item | Why it is parked | Trigger |
|---|---|---|
| **A theme change dirties the repo** | `.register/config.json` is tracked — §08 P8 names only `fonts/` and `trash/` for .gitignore — so flipping to dark shows as a modification in a vault under git. Defensible either way: your scheme is vault state and might reasonably travel between your own machines, but it also pushes your theme at a collaborator. Pinned by a test so the choice stays deliberate. | A shared vault, or anyone irritated by a dirty status after touching settings. |
| **The scheme has no third button** | §02b Screen 6 draws `[ Light ][ Dark ]` and no "system", so pressing the lit one returns to following the OS. That is the only way back to the default and nothing on screen says so beyond a line of hint text. | A §02b revision that draws the control with three states. |
| **BYOF is one face, not a family** | One licensed file per vault, registered as `TX-02` with whatever weight it carries. A user who owns regular *and* bold gets only one of them, and the app synthesises the other. §03 describes the loader in the singular; supporting a family means naming weights in the UI. | Anyone loading a licensed face and finding bold looks wrong. |
| **~~`register init` does not commit~~** | ~~Closed.~~ `--git` now makes a `vault: initial` commit, so a fresh vault reads CLEAN rather than as a wall of untracked files. Only a repository `init` created itself: point it at a folder where you ran `git init` and it commits nothing, because your staged work is not ours to sweep up. A machine with no configured identity gets a note saying so instead of a failure. | — |

## Parked during P10 (release engineering)

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~`docker compose up` is unverified~~** | ~~Closed.~~ Verified after the machine's Docker daemon was restarted: the image builds to **3.25 MB** on arm64 and **3.65 MB** on the amd64 runner that publishes it, `docker compose up` serves `:7777` against the mounted vault, a `PUT` through the container lands on the host filesystem, and — the part most likely to have been broken — the watcher inside the VM sees a **host** edit through the bind mount in **15 ms**. | — |
| **~~The published image is amd64 only~~** | ~~Closed.~~ One manifest now carries `linux/amd64` and `linux/arm64`. Not built under QEMU — `binaries` already cross-builds a static musl binary per platform on its own native runner, so `deploy/Dockerfile.release` compiles nothing and `COPY`s the one buildx asks for; adding the platform cost no build time. The cost is a second Dockerfile, and the half nobody develops against is the half that ships — so `tests/release.rs` compares their `USER`, `VOLUME`, `EXPOSE` and `ENTRYPOINT` line for line. | — |
| **~~No `latest` image tag is published~~** | ~~Closed in v0.4.2.~~ The trigger named here — *"a decision that the convenience outweighs the doctrine"* — is exactly what happened: someone tried to install this and could not, and the maintainer ruled that rule 11 governs what the project *consumes* rather than what it publishes. `latest`, `v0.4.3` and `0.4.3` are all pushed, `nothing_consumes_a_tag_that_moves` still fails the build on a consumed floating tag, and the compose file keeps a version pin that a test holds to `Cargo.toml`. See the fuller entry under *Decided after v0.4.1*. | — |
| **~~The release workflow has never run~~** | ~~Closed.~~ Six tags have now run it — v0.3.0 through v0.4.3 — and the thing that was untestable locally, GitHub itself, has been exercised: all nine jobs green, five binaries plus `SHA256SUMS` attached, and a multi-platform manifest pushed to ghcr. The emulated `apk add` for arm64 (added in v0.4.3 so the published image carries git) runs there too. | — |

## Parked during P11 (QA hardening)

| Item | Why it is parked | Trigger |
|---|---|---|
| **No §02b screenshot baselines** | §02b asks for "a Playwright screenshot story per screen, light and dark, diffed against a committed baseline". Playwright is now here and one screenshot is committed for the README, but per-screen visual diffing is a separate discipline: baselines are platform-specific, and a suite that fails on a font-rendering difference between macOS and CI teaches people to ignore it. | A decision about where baselines are generated — almost certainly a container, so one platform owns them. |
| **~~The e2e budgets are measured on one machine~~** | ~~Closed.~~ The `e2e` job has run green on GitHub. The runner measured ~2.4× slower across all three latency budgets at once — a CPU ratio, not a regression — so `budgets.spec.ts` scales the assertion by 2.5 under `CI` and prints the raw figure either way; `BUDGET_FACTOR=1` runs the promise as written anywhere. The 50 MB idle-RSS budget is unscaled and passes as it stands. | — |
| **~~Frontmatter is shown in the editor~~** | ~~Closed by the ruling it was waiting for.~~ The trigger was "a §02 ruling on whether folding counts as hiding the source", and §12 turns out to state the test rather than a prohibition: *"markdown stays the literal source. Anything that hides the source must still pass §02."* Hiding is permitted on a condition, and the condition is that the fields do not stop being editable. It is not on the page at all now, and PROPERTIES — which already listed every field — edits them. **A fold row was built first and removed the same day**: a collapsible control on the one surface that exists to have nothing on it, drawing attention to the thing it was there to get out of the way. The pane shows each line as written rather than as read, because a value shown unquoted would be written back without its quotes; it writes through the buffer, so a property change takes the same debounced save, etag and conflict path a keystroke does; and it refuses `id` and `ref`, which §04 calls immutable. The block stays on screen whenever the pane could not stand in for it. | — |
| **`register serve` has no `--open`** | Every run prints a URL you then click. Trivial to add, and one more thing between clone and running. | The "stranger goes clone → running in two commands" claim being tested on an actual stranger. |

## Parked during P12 (checkpoints + remote mode)

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~GIT is permanently dashed~~** | ~~Closed by P12.~~ The tree envelope carries `{clean, ahead}` and §02b Screen 1's GIT field shows CLEAN / DIRTY / N AHEAD, or `—` when the vault is not a repository. | — |
| **A checkpoint sweeps the staging area** | It runs `git add -A`, so anything you had part-staged with `git add -p` joins the checkpoint. Off by default, and the alternative — committing only what the app touched — needs a list of what the app touched, which the watcher has but the committer does not. | Anyone using the index deliberately in a checkpointed vault. |
| **~~`git status` runs on every tree fetch~~** | ~~Closed.~~ `Vault` holds the last answer for 250 ms, which collapses a burst of tree fetches; a non-repository still costs one `rev-parse` and no more. The window is a backstop rather than the guard — the watcher calls `forget_git()` the moment the vault changes, so the staleness a TTL alone would allow (fetch, save, fetch again inside the window, read the pre-save state) cannot happen, and the client's refetch *is* triggered by that event. What the window covers is the change the watcher cannot see: a `git add` or `git commit` run by hand touches `.git` and no `.md` file. Held on `Vault` rather than in a module-global, so two vaults cannot answer for each other and a parallel test cannot evict another test's answer — which is what makes the three tests deterministic rather than hopeful. | — |
| **Remote mode has no TLS** | Deliberate — §07's pattern is a tailnet, which is already encrypted, and terminating TLS is a reverse proxy's job. But a user who binds `0.0.0.0` on an untrusted network sends their notes and their token in clear. The token gate cannot tell those cases apart. | An encrypted-remote milestone; §12 already lists "E2E-encrypted remote". |
| **The token still has no revocation list** | ~~Partly closed:~~ `--token-file` and `REGISTER_TOKEN` mean it need not be an argv string visible in `ps`. What remains is the model — no accounts, no user table, no revocation, so rotating it means restarting the server. Right for a tailnet; thin for anything more exposed. | The same milestone as TLS. |

## Parked during P13 (§02b Screen 4)

Screen 4 was normative in §02b and had no phase in §08 — the seventh of "seven
screens make v1", built last because nothing scheduled it. What it left behind:

| Item | Why it is parked | Trigger |
|---|---|---|
| **The merge table has no j/k traversal** | Every cell is a button, so Tab reaches all of them and the dashed focus ring is correct — but §02b's nav-row model is `↑↓ / j–k` down a single column, and the table is a grid. Two axes need a traversal §02b does not draw, and inventing one here is inventing where the section says do not. | A §02b revision that draws keyboard movement for a two-column surface. |
| **No "take every line from one side"** | On a note an agent rewrote wholesale, every line is a decision. The frame draws a per-line picker and a gate, and nothing else — and a bulk control is the one thing most likely to be pressed without reading, which is the failure the gate exists to prevent. | A real merge long enough that the picker is the bottleneck. |
| **The diff is line-level, not word-level** | A line with one word changed shows as two whole lines. Correct, and coarse. Word-level highlighting inside a contested row is presentational and would not change what is written; it is also a second diff to keep honest. | Someone merging prose rather than structure. |
| **`--signal` has a ninth consumer** | `UNRESOLVED` is signal-coloured, the same weight `External edit` carries, on the same argument: nothing else in the chrome means "your writing is somewhere you have not looked". §02 permits one signal colour and this is still it, but the token's own comment has read "status LED only" since P1 and has been false since P4. | A §02 ruling on what the accent is *for*, which would settle all nine at once rather than the newest. |
| **~~The vault contract still describes the manual route~~** | ~~Closed at your instruction (Rev S).~~ The instruction was true and stays first and unchanged, because an agent cannot click Screen 4 and merging the files by hand is still exactly what one should do. What is added is that a human has a second option — worth knowing precisely because an agent that merges a conflict its human meant to resolve on screen has destroyed the choice rather than made it. The order was also promoted from an order of words to a stated reason: merge first, delete second, because the merge being durable before the copy is retired is what makes a failure between the two leave *both* revisions on disk. `store.resolveConflict` has been written that way since P13 and `writes the merge before deleting the copy` has pinned it since — the contract simply never said why, so the test now cites it. It reaches new vaults only: `init` never overwrites, which is what makes re-running it safe, so a vault scaffolded before this keeps its wording. Editing files inside somebody's vault to fix that would be the worse of the two. | — |
| **A conflict of a conflict is refused, not merged** | Resolving takes the original's etag from the corpus. If the note moved again while you were choosing, the merge is refused and the table rebuilt — nothing is lost, both files stay, but you choose again from scratch. Re-deriving while preserving decisions means matching old rows to new ones, which is a merge problem inside a merge tool. | Anyone hitting it twice in a row. |

## Parked during P14 (the compatibility fixture)

§09's fourth testing layer — *"Compat · fixture vaults: a vault written by v1
opens unchanged in every later minor — §04 is the contract"* — was the one layer
that was neither built nor parked. It is built now: `tests/fixtures/vault-v1/` is
frozen and read by both parsers, and it found two live divergences on arrival.
What it left behind:

| Item | Why it is parked | Trigger |
|---|---|---|
| **An unreadable note is indistinguishable from an untitled one** | The other half of ADR-001, amended in this phase. A note whose frontmatter does not parse degrades to `title: null, tags: []` — which is right, since one bad note must not take `/api/tree` down, and is now a pinned contract. What is missing is any way to learn of it. Saying so in the tree means a new field in §04's envelope, a contract change under hard rule 1; saying so at startup means reading every note before the server binds, against §06's 500 ms start-to-editable budget. Both are real designs, neither is a decision to make in passing. | A §02b state for what an unreadable note looks like in the index — which would settle the envelope question at the same time. |
| **The fixture only covers what someone thought to break** | Seventeen notes now: loose fences at both ends, CRLF, a BOM, unknown keys, a duplicate key, a numeric tag, an unquoted date title, a quoted colon, a four-digit ref, no trailing newline, a frontmatter ref disagreeing with its filename, a bare note, a nested folder, a conflict copy and a populated trash bucket. Widening it after the fence bug found **no** further breakage, which is the good news and also the limit of the method: it proves what it contains. | A real vault from a real user, or any bug whose reproduction is a file rather than a sequence. |
| **~~§06's latency assertions inside vitest are load-sensitive~~** | ~~Closed, and not by moving them.~~ There were two — the fold at `< 16 ms` and the query sweep at `< 20 ms` — and both failed this week without the index changing at all. The entry guessed the answer was to re-home them in the e2e suite; measuring first said otherwise. Beside each stopwatch sat a *deterministic* assertion making the same claim better: `expect(index.sync(notes, corpus)).toBe(1)` **is** "costs one document to fold", and it is true or false regardless of what else the machine is doing. The timings were the weaker half of a pair. So the wall-clock lines are gone, the complexity assertions stay, and the browser keeps the user-visible number (⌘K open, scaled by `BUDGET_FACTOR`). Five consecutive full unit runs green. | §08 P6's "1k-note vault searches < 20 ms" is now asserted nowhere as a duration. If you want it back, e2e is the place — but it wants a number chosen for a browser, since a query there also pays for rendering the rows, and inventing one here would have been fabricating a budget. |
| **§06's idle-RAM budget measures the process that holds no notes** | **Measured, and it is a wording fix.** §06 says "Idle RAM (1k-note vault) ≤ 50 MB" and names no process; the test weighed the Rust server, which reads a file per request and forgets it, while the tab holds every body twice — verbatim in `corpus` and tokenised in the search index. On the 1k-note fixture: **server 8.5 MB, page JS heap 17.7 MB used of 26.6 MB committed.** So the tab costs about twice the server, and the budget passes on every reading of it — 8.5 of 50 for the server alone, ~26 MB for both. There is no bound being breached and nothing to engineer; what is left is one sentence in §06. The measurement now runs beside the assertion and is reported rather than asserted, because putting a number on the page half would be inventing a budget nobody has agreed. One caveat on the figure: JS heap counts objects, not the DOM, the compositor or the renderer process around them, so it is a floor for what the tab costs rather than the whole of it. | Your sentence. Three readings are defensible — name the server ("idle server RSS"), name both ("server + page heap"), or leave it whole and add a second row for the page. The numbers above fit all three, so this is a decision about what the promise means rather than about whether it holds. |

## Parked during the plate scale (§02 Rev K)

| Item | Why it is parked | Trigger |
|---|---|---|
| **The reading column is a third of an ultrawide** | 2× took it from 530 px of 3440 (15%) to 1061 px (31%), which is what made the type legible — but 1343 px of the editor pane is still empty, because `--measure` caps prose and `--frame-side`/`--frame-insp` are constants. Closing the gap means making the rails elastic (`clamp(250px, …)`), which turns §02's stated frame dimensions from constants into functions of width — a real change to normative values, where the plate scale changed none. It is also a different complaint from the one that was made: the column reads small because it *was* small, not because the space beside it is empty. | Anyone saying the frame looks lost on a wide screen once the type is right. The answer then is elastic rails with the ratios pinned, and it wants its own §02 revision. |
| **1.5× is forbidden by the pixel grid, and 2× overshoots** | The ergonomically correct step on a 34" 3440 panel is nearer 1.6× than 2×; 2× lands about a quarter larger than the angle the design was drawn at. It is still the right call, because the alternative is a blurred instrument: Departure Mono's em is exactly 11 design pixels, so at 1.5× every design pixel is one and a half device pixels. Rendered side by side at DPR 1, 11 px and 22 px are crisp and 16 px, 16.5 px and 19 px are not — and rounding to a whole pixel does not recover it, because what is fractional is the glyph scaling rather than the placement. | A DPR-2 panel, where 1.5× is exactly 3 device pixels per design pixel and crisp — but gating the available steps on the display makes the *choices* depend on hardware, which is the failure mode the auto/pin split exists to avoid. Or replacing the micro face, which is a §03 change. |
| **§06 budgets no JS-free asset** | `size-limit` weighs `dist/assets/*.js` only, so the render-blocking stylesheet is unbudgeted — and this change was almost entirely CSS. It grew 3.84 → 3.93 kB gz, which is nothing, and that is exactly why nobody would notice the day it was not. | Adding a third `size-limit` entry for `dist/assets/*.css`. That is a §06 amendment, so it needs a row in the budget table rather than a quiet config edit. |
| **The measure under-delivers by six characters** | `--measure: 68ch` renders **61.8 ch** — measured. `box-sizing: border-box` (`base.css`) puts the editor's 24 px of horizontal padding *inside* the cap, at all four consumers of the token. So the line length §02 specifies has never been the line length shipped. Not folded in here: it is a pre-existing typographic bug with its own blast radius across the editor, TODAY, settings and conflict, and bundling it would hide it inside a change about scale. | Fixing it deliberately, which is a one-line `calc(var(--measure) + 48px)` per consumer or a `content-box` exception — and a decision about which number §02 actually means. |
| **A pinned scale arrives after first paint** | `boot.js` applies the scheme pre-paint and cannot read `config.json`, which comes over `GET /api/config`. Auto sidesteps this by being the classless case — a plain media query, so an ultrawide is at 2× on the first frame — but someone who has pinned 1× on a wide display sees 2× briefly and then the pin. This is the config channel's existing behaviour, not new: `html.teletype` has always worked this way. | It becoming visible enough to complain about. The fix is the server stamping the class on `index.html` as it serves it — honest, since the server already owns `config.json`, but a Rust change. |
| **~~`zoom` × `dvh` is proven in one engine~~** | ~~Closed.~~ `playwright.config.ts` has chromium, firefox and webkit projects; **CI gates on the first two** and WebKit is a one-command local run on a Mac. Playwright's Linux "webkit" is the WebKitGTK port rather than Safari, and gating on it tests the port: it failed three specs on the runner — a document switch at 255 ms against a 250 ms allowance, a conflict copy that never appeared inside five seconds, and a wheel gesture — every one of which passes in macOS WebKit, which is what a Safari reader actually runs. Nothing about `zoom` × `dvh` turned out to differ: `scale.spec.ts` passes unchanged in all three, including the status bar sitting at the viewport's bottom edge at 2×. The viewport is restated per project rather than shared, because each `devices` entry brings its own and a project's `use` wins — a single shared viewport would have applied to Chromium and to nothing else. Two assertions did not survive the crossing and are their own entries below. | — |
| **§06's latency budgets are stated against one engine** | Found by the crossing above. Two of the five fail outside Chromium, measured in isolation rather than under suite load. `start → editable ≤ 500 ms`: **Firefox 729 ms, WebKit 587 ms** — much of that the engine's own launch-to-first-paint under automation rather than anything the binary does. `agent edit ≤ 100 ms`: **WebKit ~106 ms, 5 runs out of 5**, with Firefox on the line at ~101 ms — and that one is not launch cost, it is how fast the engine puts a repaint on screen, with identical product code underneath. The other three (document switch, idle RSS, palette) pass in all three. The assertion is scoped to Chromium rather than loosened, because hard rule 3 says shrink the change and not the budget, and because §06 names no engine at all: deciding whether the number is a claim about Chromium, about every engine, or about the binary minus the browser is a normative edit and not one to make in passing. | Your ruling on what §06's latency row means. The cheap answer is a sentence in §06 naming the engine the numbers were taken on; the expensive one is a per-engine budget table. |
| **Safari keeps controls out of the tab order, so the Tab route is Safari-only-by-preference** | Also found by the crossing. WebKit does not put buttons or links in the tab order unless the reader has enabled "Press Tab to highlight each item on a webpage" — measured, six Tabs leave focus on `<body>`, so `audit.spec.ts`'s focus-ring walk has nothing to walk and is skipped there. §01's promise mostly survives: the product's own traversal is `j`/`k`, the arrows and ⌘K, which are key handlers rather than tab stops and work everywhere. What Safari costs is specifically the Tab route — including reaching a wikilink or the frontmatter fold row, both of which were given `tabindex="0"` on the strength of Tab reaching them. | Wanting the Tab route to work for a Safari reader who has not changed that setting. There is nothing to fix in the app; the honest options are to say so in the docs, or to give the marks a non-Tab route of their own. |

## Parked after v0.4.2 (a question about the left pane)

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~The INDEX cannot show a folder tree~~** | ~~Closed by §02b Rev N.~~ Parked on the reading that a tree is the affordance that makes people file instead of link — the maintainer overruled it on the argument that settles it: at scale a flat list is a scroll, not an index, and folders are the structure every reader already knows how to operate. Built as the filesystem with no special cases, `notes/` included; folders before notes and each alphabetical; a chain holding one folder compacts to a single row, because a level offering no choice does not earn indentation in a 250px rail. A folder is a nav row and takes the §02b state matrix unchanged, with `↵` folding rather than opening. **Membership did not change** — `isListed` still governs, so the contract, the stencils and the journal stay out; drawing structure is not a decision to change what the index contains, and reversing the v0.3.2 call on the journal silently would have been one. | ~~Closed by §02b Rev Q.~~ The trigger fired: the journal is a folder that starts shut, one row until asked for, newest first, dated from the filename. `isIndexed` draws it while `isListed` still governs what counts as a note you filed — so the status-bar count and the create targets are unchanged, which is what kept this a drawing decision rather than a membership one. |
| **The index sorts by path, so refs look shuffled once folders are deep** | ~~Partly answered by Rev N~~, and worth keeping open. Grouping fixed the *appearance*: notes now sort within their folder, so a deep vault no longer interleaves refs from different places. What Rev N did not settle is the order *inside* a folder, which is still path and therefore still filename — rename a note's slug and it moves. `vault.rs:393` sorts by path, which reads as a numbered register only while every note is one level down. Add folders and the register scrambles: measured, `011` sorts between `000` and `001` because `areas/` precedes `notes/`, and `010` lands last because `notes/projects/` follows `notes/007-…`. Nothing is wrong or hidden; it just stops looking like a sequence, which is most of what the left pane is for. Sorting by `ref` instead would fix the appearance outright — but a ref is optional (`017-bare.md` in the compat fixture has none), so it needs a stated rule for notes without one, and that rule is a decision rather than a patch. Left alone because a vault of one level — which every vault is until someone deliberately nests — sorts identically under both, so the change is invisible exactly where it would be tested. | Anyone actually nesting notes and finding the index hard to read. The fix is one comparator plus a rule for ref-less notes, and it wants a compat-fixture case pinning both orderings so the answer cannot drift. |

## Parked during the embed caret fix

| Item | Why it is parked | Trigger |
|---|---|---|
| **An image that arrives *after* the first measure leaves the caret a line out** | Found while fixing the margin bug, and it is a second cause of the same symptom. Delay `/api/file` by ~900 ms and the height map keeps the pre-load estimate permanently: clicking below the image lands a line low, and it never corrects — not after two seconds, not after further measure passes. Three fixes were tried and **measured to change nothing**: `view.requestMeasure()` on the image's `load`, the same on `error`, and an empty `view.dispatch({})`. They were removed rather than left in, because code that demonstrably does nothing is worse than an absence — it reads as the problem having been handled. On localhost the image is decoded before CodeMirror's first measure, which is why the ordinary path is correct and the shipped tests pass. | A reader on a slow link, or an image big enough to decode late. Reproduce with Playwright's `page.route` delaying `**/api/file/**`; the next step is understanding why CodeMirror's height map does not re-read a widget whose DOM grew, rather than trying a fourth guess. |
| **~~Nothing stops a margin coming back~~** | ~~Closed.~~ `doctrine.test.ts` bans `margin` inside the `.cm-embed` rule, beside the motion greps that already read this file, and carries the measurement that explains it: `var(--s3) 0` is 24px against a 21px line, so a click below one image landed a line out and below two images two lines out. The ban was checked against its own absence — put the margin back and it fails — and it has an anti-vacuity guard, so deleting the rule fails it rather than satisfying it. `embed-caret.spec.ts` still catches the regression in a browser; this one says why at the point somebody would be adding it. | — |

## Parked during folder creation (§04 Rev P)

| Item | Why it is parked | Trigger |
|---|---|---|
| **A titled note still needs a stencil** | The palette's create path with a title is NEW FROM TEMPLATE; `N` and the NEW · NOTE row both make `Untitled note`. So typing `notes/projects/Launch plan` needs a stencil to land on, and every scaffolded vault has one — but a vault whose `templates/` was emptied can only create untitled notes, in a folder or out of it. Predates this work and is unchanged by it. | Anyone deleting their stencils. The fix is a NEW · NOTE row that takes the query as its title, which is a §02b decision about what the `new` command means rather than a patch. |
| **~~The client validates a path the server owns~~** | ~~Closed with the cheap fix, and it found something on its first run.~~ `tests/fixtures/paths.json` is nineteen cases read by `src/vault/tests.rs` and by `paths.test.ts`, so the mirror cannot drift without one of them going red — proven by deleting a rule from `cleanFolder`, which fails eight rows. The find: **the two sides disagree about redundant separators.** Rust's `Path::components` normalises an empty component away, so the server accepts `notes//x.md` as `notes/x.md`; `cleanFolder` splits on `/` and refuses the empty segment. Same file either way and both inside the vault, so it is a difference in strictness rather than a hole — which is why the table gives each side its own column instead of pretending to one verdict. | Wanting the two to agree exactly. Making the server refuse an empty component is a one-line change and a §04 surface decision: a request that works today would start answering 400, and no client of ours sends one. |
| **Nothing renames or moves a folder** | Deleting and creating both exist now; the third of the set does not, so reorganising a vault is still Finder's job. It is the same question the deletion section already parks: moving a subtree means deciding what happens to the `[[wikilinks]]` and `![](src)` that point into it, which is a §04 question about whether the app rewrites your prose. | Anyone reorganising in the app. Answer it with the Importers row in mind — an imported vault is the one most likely to need reshaping. |

## Parked during deletion (§04 Rev P)

| Item | Why it is parked | Trigger |
|---|---|---|
| **Nothing restores from the trash** | The notice names the bucket and Finder does the rest. A restore is a bigger feature than a delete — it needs a list of what is in there, a way to see what a bucket held, and an answer for a path whose original location no longer exists — which is a §02b screen rather than an endpoint. | Anyone actually needing to undo one. Until then `.register/trash/<stamp>/` holds the subtree at its original vault path, so the recovery is a `mv` and the notice says where. |
| **A deletion has no `If-Match`** | `DELETE /api/note` never had one and the folder route does not either, so a note edited by an agent in the second between the confirm being drawn and answered is trashed carrying that edit. Nothing is lost — it is in the bucket — but the reader agreed to a different file than the one that went. A per-note etag does not describe a subtree, and inventing one for a directory is a §04 format decision rather than a patch. | Wanting the confirm to be a real optimistic lock. The honest shape is probably a tree revision, which is a bigger idea than this. |
| **The confirm counts what the INDEX draws, which is not what leaves** | Deliberate, and the notice afterwards reports the server's own count — but it means the number in the question is always a lower bound. A folder holding twenty images says "3 notes". Showing the true count would mean asking the server before asking the reader, which is a round trip inside a keystroke. | The gap being surprising in practice rather than in theory. A `GET` on the folder route returning the tally without moving anything is the cheap fix, at the cost of an endpoint that exists only to be asked twice. |
| **An empty top-level folder is left on disk** | Pruning stops at any direct child of the root, so emptying `notes/` leaves it — deliberately, since the §04 layout must survive its own last deletion. The cost is that a folder *you* made at the top level lingers empty after its notes go, showing in Finder and nowhere in the app. | A user with several top-level folders finding the leftovers. Distinguishing "§04's own" from "yours" means naming the layout in code, which the vault format does not currently do anywhere else. |
| **A folder is deletable but not renamable or movable** | Deleting is the destructive half and it is the half that was asked for. Rename and move are the same missing primitive from the other side — there is no `MOVE`/`PATCH` on any route, so reorganising a vault is still Finder's job. Doing them properly means deciding what happens to `[[wikilinks]]` and `![](src)` that point into the moved subtree, which is a §04 question about whether the app rewrites your prose. | Anyone reorganising in the app rather than in Finder. It should be answered with the Importers row in mind, since an imported vault is the one most likely to need reshaping. |

## Parked during the media surface (§04 Rev O)

| Item | Why it is parked | Trigger |
|---|---|---|
| **A `[link](other.md)` still does nothing** | Notes are linked with `[[wikilinks]]`, and Screen 8 refuses a `.md` — it is not media. So a markdown-style link to another note is drawn as plain demoted text and clicking it does nothing at all. It was briefly worse: `fileUrl` resolves any vault-relative path, `.md` included, so the first version dressed such a link as a viewer link that would open onto its own 415. Excluded rather than made to work, because "which of the two link syntaxes opens a note" is a §04 question. | Anyone importing a vault that links notes the markdown way — the Importers row is where those arrive from. |
| **A link to a missing file stays dressed until something embeds it** | A reference goes inert when its target proves absent, and the only thing that ever finds that out is the `<img>` an embed renders — media is deliberately absent from `GET /api/tree`, so nothing else knows what is on disk. `![x](gone.png)` therefore demotes itself and every other reference to `gone.png` with it, while a lone `[x](gone.pdf)` nobody embeds stays dashed, and clicking it opens Screen 8 onto "not in the vault". That is one click of wasted motion rather than a wrong answer, which is why it is parked and not fixed. | Wanting it exact. The cheap half is a `HEAD` per reference, which spends a request per link on every note opened to save that click; the honest half is deciding whether the tree should list media at all — the same question the orphan-image and watcher-repaint rows below wait on. |
| **SVG is not served** | Deliberately absent from the allowlist. It is XML that can carry script, and while the global CSP would block that, the §03 principle this endpoint copies is "an allowlist of containers, not a passthrough" — SVG does not earn an exception on the day the endpoint is born. The cost is real: SVG is a common diagram format and a note referencing one gets the missing state. | Deciding what a safe SVG path looks like. `Content-Disposition` on that one type, or a `sandbox` CSP on the response, or rasterising — each is a different answer and none is obvious. |
| **A changed image does not repaint** | The watcher is `.md`-only (`is_visible`), so replacing `diagram.png` on disk leaves the rendered copy until a reload. Widening the watcher touches the definition the tree, the API and the watcher share by design — the thing `resolve`'s comment calls "a silent way to lose data inside your own vault" if the three ever disagree. | Anyone iterating on a diagram beside the app. The cheap half is an `etag`-busting reload of that one `<img>`; the honest half is deciding whether media belongs in the watcher at all. |
| **An orphan image is invisible** | The INDEX is a register of *notes*, and `isListed` still governs, as Rev N said when the tree landed. A file nothing references can only be reached through Finder. | Wanting an attachments view. That is a §02b screen, not a filter. |
| **`![[embed]]` is not read** | Obsidian's embed syntax, which §04 does not define. Standard `![alt](src)` is what the grammar already parses and what the tree already reports. | The Importers row — Obsidian vaults are where this syntax arrives from, so the two should be answered together. |
| **The start-to-editable budget runs close to its limit on a loaded machine** | Measured while building this: 7 of 8 runs passed at HEAD *before* the image decoration, 8 of 8 with it, with the failures at 501–568 ms against a 500 ms limit. So it is not a regression from anything here — it is that a developer machine mid-build sits near the line. CI is unaffected: §06 scales the assertion 2.5× there, for exactly this reason. | It failing on an idle machine, which would make it a real regression rather than load. Until then the honest reading is that the figure has little headroom and a change that costs 50 ms would not be visible as a failure until it costs 100. |

## Parked after the folder tree

| Item | Why it is parked | Trigger |
|---|---|---|
| **~~The frame overflows a window narrower than it wants~~** | ~~Closed, and the entry it replaces was wrong twice over.~~ It said the overflow was pre-existing and that HEAD had improved it. Both claims came from one bad measurement: a resize sequence that opened a *different note* on each build, so the two sides were never compared like for like. Measured properly — same note, same widths, fresh load — the published v0.4.2 has no overflow at any width, and HEAD before this fix drew the frame **1080px wide inside a 900px window** with the clock, INV and ⌘K past the right edge and `html{overflow:hidden}` leaving no scrollbar to reach them. **It was a regression I introduced with the folder tree, not an old bug.** The cause: `.app` declared only its rows, so its single implicit column was `auto` and sized to the widest row's min-content — the header and status bar are `white-space: nowrap` and measured 1095px and 1728px. `grid-template-columns: minmax(0, 1fr)` lets the track shrink to the frame, exactly as `.mid` has always declared its own, and each row then clips inside its own `overflow: hidden`. The crumb split was ruled out by measurement rather than by argument: reverting it leaves the overflow untouched. | — |
| **The crumb's "trail gives way, the note does not" rule is unfalsifiable in practice** | Still true, and now for a better reason: with the frame fixed, the header narrows properly, but the crumb slot still does not fall below the trail's width in the band where it is drawn. The CSS is right and is asserted, but only as a mechanism — `flex-shrink` on each half — because the outcome cannot be reached. In the band where the crumb is drawn at all (`display:none` below 760, header stops narrowing near 1080) the crumb slot never falls below ~577px, while the deepest trail in the fixture measures ~457px. So nothing clips at any reachable width. Written down rather than left as a passing test that could never fail, which is the shape that was already removed from `treeTraverse` once in this session. | The overflow entry above being fixed, which would let the header actually narrow — at which point the outcome becomes reachable and the test should assert it rather than the mechanism. |

## Parked during the frame audit (§02b fidelity)

A sweep of SPEC against the code looking for the opposite of a bug: things the
frames *draw* and the app never grew. Six, and all of them surface — five §02b
rows and one §03 face. What shipped: Screen 1's document header and meta strip,
Screen 3's missing two lines, Screen 7's `+1` and `WATCHER` readouts, the hover
state for wikilinks and file references, a keyboard route to a link, and Server
Mono's oblique. Also one defect found on the way: `.register/config.json` lost
every key §02b Screen 6 does not draw — including `"checkpoints": true` — the
first time anyone flipped a theme or folded a folder.

The entries below are what those six left behind. Two of them are deliberate
deviations from a normative frame, and a deviation nobody writes down is
indistinguishable from an oversight.

| Item | Why it is parked | Trigger |
|---|---|---|
| **Screen 1's meta strip is not built at all** | The frame draws `NOTE · REF 003 · REV 07` over `┌created┬modified┬words┬status┐`. Two of those cells never could be built: §04's note format is `id / ref / title / created / modified / tags`, so there is no revision number and no status anywhere in a vault, and §02b's own fidelity rule forbids "a gauge [that] display[s] a number the system cannot measure" — in `register-prototype.html` both are literal fixture strings (`REV 07`, `◉ ACTIVE`), a shape rather than a field. The other two were built and then removed the same week, by the maintainer, on the ground that the strip earned none of the room it took: `created` and `modified` became editable rows in the inspector, which put the same two values on screen twice, and `words` and `chars` are instrument readouts that belong in the status rail — §08 P4 names them and RENDER in one breath. What is left above a note is its ref and its title. | A §02b revision that redraws the row, or a §04 revision adding `rev`/`status` and giving it something to say. Until then the frame draws a strip the product does not have, and this entry is the record of why. |
| **~~The title is now on screen three times~~** | ~~Down to two, which is the frame's own count.~~ The third was the raw `title:` line, and the P11 entry above closed by folding it away. What remains is the crumb and Screen 1's title row, and §02b draws both deliberately: the crumb answers *where is this file* and the title row answers *what is this note*. | Nothing. If two ever reads as one too many it is a §02b question about the crumb, not about the note. |
| **`⌘↵` is a binding §02b does not draw** | The state matrix's Wikilink and File reference rows spend their Focus column on the missing-target appearance, so no keyboard activation is drawn anywhere — while §01 promises "every action reachable without a mouse" and P5's done-when is "full session possible without a mouse". Taken the same way the INDEX entry keys were, and for the same reason: the alternative is a documented hole in §01's central claim. Named in ⌘K so the key is on screen, exactly as `FOCUS · INDEX` is. Enter and Space on a tabbed-to mark are not new bindings — the marks already carried `role="link"` and `tabindex="0"`, and Tab already reached them; only the answer was missing. | A §02b revision that draws keyboard activation for those two rows. It should settle Space at the same time, and whether a focused mark deserves the dashed ring this now gives it. |
| **⌘K offers FOLLOW · LINK whether or not the caret is on one** | Every other command hides when it cannot act. This one cannot find out: the caret belongs to the editor, and the palette is its sibling with no channel back. So it is offered whenever a note is open and the editor answers `No link at the caret.` — said rather than silent, which is the §02 voice, but still a row that is sometimes inert. | A view-side channel reporting what the caret is on. That is the same machinery an inspector row describing the caret would want, so the two should be answered together. |
| **A watcher burst reports notes gained, not files touched** | §02b Screen 7 draws `+1` against a vault going from six notes to seven, so the delta is a count of notes and that is what it counts. An agent rewriting three existing notes changes no count: the bare cell is absent and the render cell reads `WATCHER 4.1ms` with no number. Honest, and it undersells the burst that is most likely to be interesting. | Wanting "3 files changed". That is a different number and the event stream would have to carry it — the frames are per-path, and counting them means the client holding a burst open long enough to tally it, which is the coalescing window the 100 ms budget already spends. |
| **The pane edits fields but cannot add or remove one** | `setField` appends a key that is missing and has since P2, so the store can already do it — what is absent is anywhere to type a new key's name, and any way to delete a line. So a note that needs a field it does not have can only get one from an agent, or by editing the file outside the app. Deliberate for now: a form that can add keys wants an opinion about which keys are legitimate, and §04 does not say. | Wanting a field §04 does not define — the first person to keep `status:` or `source:` in their own notes. It is a §02b question about what the pane's own empty row looks like. |
| **A hidden block has no route back onto the page** | When the frontmatter is hidden it is genuinely gone from the editor: there is no key, no command and no reveal. Every field is in the pane, so nothing is unreachable — but the *shape* of the file is, and a reader who wants to see what an agent actually wrote has to open it in a terminal. Left out rather than parked-with-a-plan, because the fold row was exactly that route and it was the thing that had to go. | Someone needing to see the literal block often enough to say so. The answer is probably ⌘K rather than anything drawn on the note. |
| **The config's foreign keys are read once, at boot** | The settings screen now carries back every key it does not own, so a PUT no longer erases what it cannot draw. It learns them from the one `GET /api/config` it makes at startup — so a key an agent adds to `config.json` *while the tab is open* is still lost by the next theme flip. Re-reading before every write would cost a round trip per folded folder. | Anyone editing `config.json` beside a running app. The honest fix is the server merging rather than replacing, which turns §05's PUT into a PATCH and is a §04 surface change rather than a patch. |

The counts live in the status rail. §02b's own status line — `● WATCHER LIVE ·
VAULT ~/vault · RENDER 0.7ms · n files · GIT` — draws neither, so this is an
addition the frame does not draw, taken for §08 P4's "Words/chars + RENDER ms
live" and sitting where the `N unresolved` button already sets that precedent.
They are drawn only while a note is open, so TODAY, SETTINGS and the media
surface show no count, which is right: none of them is a note.

## Decided after v0.4.1 (somebody else tried to install it)

| Item | The decision, and why | What would reopen it |
|---|---|---|
| **The only documented way in was to clone it and compile** | Fixed. `deploy/docker-compose.yml` had a `build:` and no `image:`, so trying the product meant a checkout plus node, pnpm and a Rust release build inside Docker — which a first-time reader reasonably took to mean "you build this yourself". It now pulls a published image and has no `build:` at all, so the quickstart *cannot* compile; contributors add `deploy/docker-compose.build.yml` explicitly, which also retags the result `register:source` so a local build cannot sit in the image store pretending to be the release. The README leads with a single `docker run` and the source build moved below it. | — |
| **`VAULT_PATH` defaulted into the checkout** | Fixed, by removing the default rather than changing it. `${VAULT_PATH:-./vault}` resolves against the compose file, so forgetting the variable put a real vault in `deploy/vault/` — one `git add -A` from being committed, and the `.gitignore` entry for that path is the evidence it happened. It is now `${VAULT_PATH:?…}`: compose refuses to start and prints what to set. A named volume was considered and rejected — §04's premise is a folder you can open in a terminal, and hiding it inside Docker's storage would break the product to tidy the file. | — |
| **Setup was two steps and only one was documented** | Fixed by making `serve` scaffold a folder holding no vault (§04 Rev M). One command is now the whole install. See that revision for why "holds no vault" means no `.register/` and no markdown at any depth. | — |
| **`latest` is now published** | The rule against floating tags is about what this project *consumes* — a `FROM` or `uses:` that moves under us is an unpinned dependency — and says nothing about what it publishes. Somebody trying the product should not have to find a version string first. `nothing_consumes_a_tag_that_moves` still fails the build on a consumed floating tag, with the published name exempted by exact string. The compose file keeps a version pin, and a new test fails the build if that pin falls behind `Cargo.toml` — the drift that would otherwise hand every new user the previous release while the README claimed otherwise. | Anyone treating `latest` as stable in automation. The versioned tags are what to depend on and the README says so. |
| **~~The published image has no git, and that is now the default path~~** | ~~Closed, by paying the cost rather than documenting it.~~ It was parked on the grounds that `apk add` under QEMU was too much for one status field — but making pull the default put that field's failure on the newcomer's path, which is the one that matters most. `Dockerfile.release` is now the same `alpine:3.24` + `git` runtime the developed image uses, and `release.yml` installs binfmt for the single `RUN` it takes. The emulation is scoped to the package install; the Rust build stays on native runners in the `binaries` matrix, which is the property the `scratch` image existed to protect and the only one worth protecting. Measured before shipping: both platforms build, and `git --version` and `/register --version` both answer on each. The two Dockerfiles now share a runtime base, asserted by comparing their last `FROM` — which found that the helper doing the comparison took the *first* matching line, so it had been reading the developed image's UI builder. | — |

## Decided after v0.4.1 (an ultrawide monitor)

| Item | The decision, and why | What would reopen it |
|---|---|---|
| **The GIT field could never say anything in a container** | Fixed by giving the *developed* image a git to shell out to; the published one keeps `scratch`. **It was not an unknown**: `StatusBar.svelte`'s header comment had said "the container is `FROM scratch` and has no git binary, so it dashes there however the vault is stored" since 2026-08-07 (`7a35553`). It was written down in the one place only someone editing that component would read, never surfaced to a user, never given a roadmap entry, and never tested — which is how a known limitation and an unknown one become the same thing in practice. §02b Screen 1 draws GIT, §04 has `GET /api/tree` carry it, and §07 recommends the container — but `git::status()` shells out to the `git` executable and a `scratch` image has none, so `is_repo()` was false whatever the repository was doing. Measured: the same binary against the same vault at the same moment reported `{"clean":false}` on the host and `null` in the container. `deploy/Dockerfile` is now `alpine:3.24` + `apk add --no-cache git` — 3.41 MB → 25.7 MB, accepted deliberately, because a permanently dashed field is a dead pixel with a label. `deploy/Dockerfile.release` **followed it**, which this entry claimed for a while that it had not: keeping the published image `scratch` would have meant recommending a container in which the field never works, and pulling a published image is the documented way in. The price is real and is paid deliberately — its one `apk add` runs under QEMU for the non-native architecture, and `release.yml` installs binfmt for exactly that line. The two runtime stanzas are therefore identical, and `both_images_carry_git_and_the_release_can_emulate` compares them line for line so the copy cannot drift from the one people develop against. **What that changed and nobody wrote down**: the published image stopped being a binary and became an operating system, so it now redistributes Alpine — musl, busybox, git, apk-tools, and their GPL-2.0 terms. `THIRD-PARTY.md` said `scratch` and "redistributes nothing the binary does not" for as long as that was untrue, which for a licensing document is the one sentence that has to be right. | Wanting GIT to work from the published image too — the honest options are a second published tag, or accepting QEMU in the release build. Also: nothing tested any of this, because the e2e suite runs the binary on the host where git exists, so the container's own behaviour is still only checked by hand. |
| **The GIT field did not speak git** | Changed to git's own shorthand (§04 Rev L). It read `CLEAN` or `DIRTY`, and `--dirty` is a git *flag*, not a word git prints — while the pair could not tell a vault with one untracked note from a vault mid-rebase. The data was already being fetched and thrown away: `status()` ran `git status --porcelain` and kept only whether the output was empty. So the envelope grew to `{branch, clean, staged, modified, untracked, ahead}` and the field is drawn the way `git status --short` and every shell prompt draw it — `MAIN`, `MAIN ~3`, `MAIN ↑2 +1 ~3 ?4` — with a mark only when its count is above zero. `clean` stays, and stays derived from the raw output rather than from the counts, so the two cannot drift apart silently. Additive, so a client reading only `clean`/`ahead` is unaffected; no file in the vault changes shape. | A field that needs to distinguish *behind* from *ahead*, or to name a remote — both are more envelope, and the rail is 30px shared with five other fields. |
| **Checkpoints cannot commit from the container** | **Half closed, which was the half named "worth having regardless".** It no longer fails *silently*: `checkpoint()` answers `Committed | Nothing | Refused(reason)` and a refusal goes to stderr carrying git's own message, which for this case is the one that tells you how to fix it — *"Please tell me who you are"* and the two `git config` lines under it. Measured against a vault with no identity, and `docker logs` is where an operator will be looking. What remains is the identity itself: `git commit` needs `user.name`/`user.email`, the image sets neither, and inventing one for somebody's history is still not a default. | Passing an identity through compose, which is a `deploy/` decision rather than a code one — an operator who sets `GIT_AUTHOR_*` in their own compose file already has it working today. |
| **The frame rendered at exactly one size on every display** | Fixed, as a **plate scale** (§02 Rev K). Every §02 dimension now describes one plate unit and the product renders at whole multiples of it — 1× and 2× — applied as `zoom` on `<html>`, the same channel as `html.dark` and `html.teletype`. **No normative value moved**: `--text-body` is still the literal `13px`, and 1× is byte-identical to Rev J, which is why the frame-geometry assertions pass unmodified. The alternative, rewriting each token as `calc(13px * var(--scale))`, would have edited §02's numbers, double-scaled `--measure` (in `ch`) and the trackings (in `em`), and demanded a judgement call on all 26 addendum tokens — for an identical rendered result. Two questions were separated rather than answered together: *how big* is the user's, stored in `config.json` beside `scheme` and `bodyFace`; *is there room* is the canvas's, settled by one media query. That split is what makes a stored scale safe at all — §07 serves one vault to every device, so 2× chosen on the ultrawide is read verbatim by the laptop, where it is vetoed rather than obeyed. The breakpoints moved from `@media` to `@container` so they measure the frame in plate units instead of the raw viewport. | A display class the two stops do not serve — 5K/8K, where 3× is a one-word addition, or the 2000–2500 px band, which gets nothing and cannot get 1.5×. |
| **Nothing in CI had ever rendered the app wide** | The widest viewport any test used was 1600, and everything else ran at the config's 1400×900 — so a frame that drew a 530 px column into 3440 px of screen was invisible to 27 e2e tests and 442 unit tests. `audit.spec.ts`'s rule-alignment loop now runs at 3440 and 2560 as well, which is the single cheapest assertion that would have caught the original complaint, and it lives there rather than in `scale.spec.ts` because rules falling out of register is that test's failure. | — |

## Ruled on after v0.6.3 (twenty-one in one sitting)

Emptying the worklist's "I can do these now" left twenty-one items that needed a
decision rather than a day. They were gone through one at a time and answered.
Each row is the ruling and the argument that decided it — recorded so the same
question is not re-argued, and so a future disagreement argues with the reason
rather than with the outcome.

| Item | The ruling, and what decided it | What would reopen it |
|---|---|---|
| **NEW · NOTE discarded the title you typed** | Built. It made `Untitled note` and threw the query away, so a titled note could only be cut from a stencil and a vault whose `templates/` had been emptied could create nothing else. Two things were wrong, and the second was the real one: the row **vanished while you typed**, because `Launch plan` does not fuzzy-match `NEW · NOTE`, so the only row that could act on what you had written was the one that disappeared as you wrote it. It is exempt from the match now for the reason the stencils already were — the query is its argument, not a filter over it — and sorts after every exact match so it never displaces a note you were looking for. | — |
| **A tag was not clickable** | Built: a click seeds the palette with `tag:name`. §02b defines no tag component, so a filter over the tree would have meant inventing a state, a second browsing mode, and an answer for folders left empty — where the palette's states are already drawn and it already lists notes. `tag:` is an **exact** filter rather than a search, because free text also finds every note *mentioning* the word: "what is tagged research" and "what is about research" are different questions and the click has to mean the first. | Wanting the filter to persist while browsing, which is the tree mode this declined. |
| **The delete confirm undercounted** | Built, as wording rather than arithmetic. The count is what the INDEX draws and the INDEX draws notes, so a folder holding twenty images asked about three — and a bare number reads as complete. It now says "N notes, and anything else in the folder", which is true, instant, and tells the reader the fact a true count would still have hidden: that the extras are their images. The alternative was a `GET` on the folder route inside a keystroke, on the one interaction where hesitation is worst. | — |
| **An unreadable note looked like an untitled one** | Built: it says so when the note is open. The degradation — no title, no tags — is right and is pinned by contract, but it was drawn exactly like an ordinary untitled note, so it was silent and permanent and the note's tags were quietly missing from the tag index. Said at the note rather than in the index, so §04's envelope does not move and §02b gains no index state; and **derived rather than reported**, from three facts that only a parse failure produces together — the file has frontmatter, that frontmatter has a `title:` line, and the server still says there is no title. | Wanting the index to be a health view, which is the §04 envelope change this avoided. |
| **Any loopback origin was trusted** | Built: `--dev-origin`, and the guard otherwise accepts only the origin the app is served from. The exemption existed so `pnpm dev` could proxy from vite on another port, and it gave the same authority to **every other web server on the machine** — a page open in a tab on `http://localhost:3000` could read the vault, write to it and delete from it. No shared host, no hostile file, just something else listening: the most exploitable thing on the list. A hole in every install to spare contributors a flag is the wrong trade, so it is a flag. Verified against a running server: same origin 200, another local port 403, vite 403 without the flag and 200 with it. | — |
| **Loopback was read as "the owner"** | Kept, and said where it is seen. It is the standard local-server posture and is what makes `register serve ~/vault` the whole of setup — but on a shared host every other account can reach 127.0.0.1, so they get read, write and delete on a vault whose mode 700 is keeping them out at the filesystem level: the app handing over what the filesystem denies. The warning moved out of `SECURITY.md`, which the people at risk will never read, into the startup banner — **conditioned** on the machine plausibly having other people on it, because a line printed every time is wallpaper. The detection is deliberately weak and errs towards silence, and can never fail the start. | Anyone wanting the real fix, which is a Unix socket and peer credentials — a §04 transport rather than a patch. |
| **Two tabs could take the same ref** | Built: a write whose ref is held by a different note is refused with 409, inside the same lock the etag check uses. The server allocates `nextRef`, so one client cannot reissue a ref — but two tabs reading the tree in the same instant both receive `015`, type different titles, and land on different paths, so the free-name check passed for both and a `[[015]]` link resolved to whichever the index reached first. Verifying what the client assumed is the shape the server already has, so this needed no route: a `POST` that allocates would have meant two ways to create a note. The client loses the race by refetching and taking the next ref — **once**, because a second failure is not a lost race. Only on creation: a note rewriting itself holds its own ref, and a daily log and a conflict copy have none. A vault can still *contain* duplicate refs, because an agent editing files directly does not get a vote taken away from it. | — |
| **A checkpoint swept the staging area** | Built: it stands aside when the index is dirty and says so, resuming on its own once you commit. `add -A` took work you had staged with `git add -p` into the app's bookkeeping commit, which destroys something you were composing. Read from the porcelain already in hand rather than by asking git again — and `??` is explicitly not staged, or a vault being written in would stand every checkpoint down forever, which is the failure this rule invites. | — |

| **The measure under-delivered by six characters** | Built: `--measure` still means 68 characters of *text*, and a `--measure-box` token adds the horizontal padding back at the five surfaces that pad by `--s5`. `box-sizing: border-box` had been taking the editor's 24px either side out of the cap, so every prose surface rendered **61.8ch** — measured — and the line length §02 specifies had never once been the line length shipped. `ch` is a typographic unit; 68 of them cannot mean "68 minus whatever padding is applied". The conflict screen is deliberately not a consumer: it pads `var(--s5) 0`, so its text was already right. Visible reflow everywhere prose is drawn, which is the point. | — |
| **The reading column was a third of an ultrawide** | Built, and **not** the way it was first written. §02's rails are the base and they widen in discrete steps at the plate scale's own thresholds. The first attempt was `clamp(250px, 14cqw, 460px)`, which broke the frame: the header takes its rail width from `min-width` and the panes take theirs from a grid template, and WebKit resolved `cqw` differently for the two — the header rule at 500 against a pane edge at 920, measured — so §02's "vertical rules continuous through the header" stopped being true, on the one engine CI does not gate. A container-query unit read from two different boxes is one value too many for a frame whose aesthetic is that its rules line up. Steps cannot disagree, and they match how the plate scale already works. Prose is untouched: it is capped by `--measure-box`. | A display class the two steps do not serve. |
| **`--signal` had nine consumers and a comment that named one** | Written down rather than repainted: the accent means *the live edge of the system* — where you are, and what changed under you. The comment read "status LED only" from P1 and had been false since P4, so each new use was argued against the count rather than against a rule and the tenth had nothing to meet. All nine fit; a doctrine test now holds the rule in the file somebody reaching for the colour will read. Repainting under either narrower rule would have declared half the existing UI wrong for no gain. | A use that is neither where you are nor what changed. |
| **The etag could collide** | Kept `mtime + len`; promoted the mitigation into §04 as normative — a client receiving a `changed` frame MUST reload the note even when the tag it holds is equal. Then found the client did not obey it: `#isEcho` treated an equal tag as "our own write" and skipped the reload, so a colliding write would have been dropped silently, which is the one way the collision loses writing rather than being untidy. The open note now reloads on a clean buffer regardless of the tag, at a cost of one GET after our own save; the tag still answers the *timing* question, which is a different one. Hashing every note on every read would close the window at a cost §06 has no room for. | A measured collision on ext4, which would argue for the hash after all. |
| **Slug folding was partial, not NFKD** | Built by keeping the characters rather than by adding a crate. Letters and numbers in **any script** survive; punctuation still folds, so every ASCII title slugs exactly as it always did. `register new "Заметки"` wrote `015-untitled.md` while the browser produced a real name from the same title — one title naming two files depending where it was typed. Combining marks are stripped only after a **Latin** base: applied to every script it took the tonos off `Ελληνικά` and produced `ελληνικα`, which is not the word, and which the CLI's Latin-1 fold does not do — so the two sides would have started disagreeing on Greek in the same change that stopped them disagreeing on Cyrillic. Verified on a real vault, both sides. | A vault that needs transliteration rather than preservation. |

| **SVG was not served** | Built, sandboxed. Rev O's principle was right — "an allowlist of containers, not a passthrough" — and the price turned out to be the output format of every diagram tool anyone uses: Excalidraw, Figma, Mermaid, draw.io, all showing "not in the vault" with the file sitting there. It is XML that can carry script and it comes from this origin, so that one response carries `sandbox; default-src 'none'`: no script, no plugins, no forms, no navigation, no same-origin access, nothing to fetch with. Per-response, as this route already relaxes `frame-ancestors` on itself — and the frame-wide header layer now **defers** to a policy a handler set, which it previously overwrote, so the tightening actually reached the response that needed it. Recognised by its **root element**, never by "contains `<svg`": an HTML page can hold one, and serving HTML from this origin is what the allowlist exists to stop. Thirteen accepted shapes and seven refused ones are pinned, including `<html><body><svg/></body></html>`. | A format that wants the same treatment; the shape is now established. |
| **A theme change dirtied the repo** | Built as a split, and the ruling was the interesting part: the file had become two different things. `.register/local.json` (gitignored) takes the scheme, body face and plate scale — the machine you are sitting at, and the app already vetoes a 2× scale on a laptop — while `config.json` keeps the collapsed folders, the journal's fold and the checkpoint flag, which describe the content and *should* travel. Ignoring the whole file loses the second half; tracking it kept losing the first. **No migration step**: the machine keys fall back to the tracked file until the local one carries them, and the next save moves them across and stops writing them to `config.json` — so a vault made before this keeps the theme it was given and migrates itself the first time anything changes. An upgrade that silently reset everybody's theme would have been a worse bug than the one being fixed. Verified on a real git vault: a theme change now leaves `git status` empty, and a folder fold still shows as a diff. | The `.gitignore` line reaches new vaults only, which is what "init never overwrites" costs. An existing vault needs the one line by hand. |

| **A deletion had no `If-Match`** | Built as a **tree revision**, which was the more ambitious of the two options and needed one thing to be usable. `GET /api/tree` carries `rev`; both delete routes take it as `If-Match` and answer 409 with the current one. It is in-process and sound *because of the claim* — one server holds a vault, so there is exactly one counter, which is the second thing that mechanism bought. Bumped by every write this process makes and by every change the watcher sees, since an agent's edit moves the vault exactly as our own save does. Absent means unguarded, deliberately: `curl -X DELETE` is documented and cannot have read a revision. **The confirm re-asks rather than failing**, and that is the thing that makes the guard usable rather than an irritation — any write bumps the revision, including the reader's own debounced save, so a confirm drawn a moment earlier would otherwise refuse against nobody but themselves. On a 409 the palette refetches, redraws the count and puts the question again. **The revision moves in exactly one place**, and that was the second thing this needed: bumping it on the write path *and* in the watcher counted one edit twice, so a re-armed confirm went stale a moment after being redrawn and the reader was refused twice for one change — surfaced as a WebKit-only e2e failure, which is that 50 ms window made visible. The watcher sees every change including ours, so one source is symmetric. | — |

| **Nothing restored from the trash** | Built: §02b Screen 9. Buckets newest first, `↵` restores, `⌦` arms a purge. Restoring recreates folders on the way, which answers the question that kept it parked — delete `notes/apollo/`, then `notes/`, and the vault grows the folder back exactly as a write does. An occupied path is **skipped, never overwritten**, and the count of what stayed comes back: a restore that clobbered the note now living there would be the one operation in this product that destroys. Purging is the other one and the only other one, and it also gives up §04 Rev F's promise that a ref is never reissued, since `next_ref` counts the trash — said out loud rather than discovered by somebody whose `[[003]]` came to mean two notes. | — |
| **An orphan image was invisible** | Built: §02b Screen 10. `GET /api/files` lists paths and nothing else — *what references what is derived on the client*, which already holds every note body, because the server answering would have meant it parsing prose to answer a question about files. A file nothing points at is drawn dim and says so, which is the honest weight: unreferenced is untidy, not broken. | — |
| **Nothing renamed or moved** | Built, and the ruling's premise turned out to be too pessimistic. It was parked on "does the app rewrite your prose", and the answer is *hardly ever*: `[[wikilinks]]` resolve by ref or title, so a move breaks none of them, and moving a folder whole rewrites **nothing** because the images travel with the notes. What is left is narrow — a note moved away from a file it points at — and `move.ts` is the whole of it, kept relative rather than rewritten absolute because §12 asks that markdown stay the literal source. The confirm names the count before anything is edited, per the ruling. Move first, rewrite second: a failure between them leaves every file on disk and a stale reference, where the other order edits notes to point at a move that then does not happen. | — |

| **§06 budgeted no CSS** | Built at 10 kB gz, against 4.4 today. `size-limit` weighed `*.js` only, so the one render-blocking asset was unweighed — in a design system where a whole change can be pure CSS, which is exactly why nobody would notice the day it stopped being small. | A number that needs raising for ordinary work, which would mean the design system had grown rather than the budget being wrong. |

### The one CI found that a laptop could not

`register serve` **died when nobody read its stdout**, and it took four CI rounds
to see it.

`println!` panics on a closed pipe — Rust sets `SIGPIPE` to `SIG_IGN`, so the
write returns `EPIPE` and the macro unwinds out of `main`. The banner line added
for the loopback warning is printed only when the host looks shared, which is
true on a GitHub runner and false on a developer machine; `tests/compat.rs` reads
one banner line and lets go of the pipe, so on the runner the *next* line met a
closed pipe and killed the server. Seventeen of them, in the same millisecond.

Three fixes went in ahead of it, all correct, none of them the cause: a reset
after a response is tolerable, an idempotent GET is retryable, and a claim keyed
on an inode must not be believed when the inode has been recycled. What kept the
search going in circles was the diagnostic — it asked whether `/proc/<pid>`
existed, and that directory survives for a **zombie**, so a server that had
exited and not been reaped was reported as "still running".

Two lessons worth the cost. **A `failed printing to stdout: Broken pipe` panic
was in the very first log** and was read as noise for three rounds. And a
diagnostic that cannot distinguish *alive* from *exited-and-unreaped* is worse
than none, because it answers confidently.

Every stdout write in `main.rs` goes through `say!` now, which ignores a broken
pipe. `register serve ~/vault | head -0` is the same shape and is pinned by
`a_server_survives_nobody_reading_its_console` — closing the pipe *before* the
first write, because the start-up lines otherwise land in the buffer before any
reader can let go, and the race that bit CI cannot be scheduled on demand.

### A note on the latency budgets

They failed five times in this session and every one was the same artifact: the
**first e2e run against a freshly written binary**. `start → editable` read 961 ms,
4520 ms and exactly 500 ms cold, then passed 4/4 and 5/5 warm; `agent edit → paint`
did the same at 110 ms. A 10 MB binary's first exec pages in from disk, and the
build-then-immediately-measure loop puts that cost inside the measurement.

Two things were nevertheless found by chasing it. The vault claim was keyed on an
inode, so a few hundred stranded lock files from killed e2e servers each cost a
`fork`+`exec` of `kill` at startup — fixed with a `stat` before the pid probe. And
the config split had put a second sequential round trip in the boot path, which is
now one `Promise.all`. Neither was the flake; both were worth fixing.

**Run the suite twice and read the second one.** A cold measurement is not a
measurement.

## Found by audit after v0.6.3 (the worklist was empty)

The "I can do these now" table had just been emptied, so the question was
whether anything was left. Five readers went over the codebase in parallel and
every candidate was handed to an independent reader told to refute it; sixteen
survived. Four had shipped in the previous week's work — three of them in the
vault claim, written the day before — which is the argument for doing this at
all: the code was reviewed, tested and green each time.

| Item | The decision, and why | What would reopen it |
|---|---|---|
| **A container could never take over its own stale claim** | Fixed, and it was the takeover path's whole purpose defeated by the one deployment it was written for. `holder_is_gone` asks `kill -0`, and under an exec-form entrypoint the server is pid 1 — so a claim stranded by a `docker kill` reads `pid 1`, the replacement is also pid 1, and a process can always signal itself. The probe answered "still running" forever and the vault never started again. A claim naming *this* process is now stale by construction, asked of a set of the paths this process actually holds rather than assumed, because inside one process a second `Vault` over one directory is still a second server and must still be refused. | — |
| **Two servers could both clear one stale claim** | Fixed. The takeover was `remove_file` then `create_new`, and `remove_file` unlinks whatever is at the path *now* — not the stale file that was inspected a moment earlier. Two servers starting together both saw the stale claim, and the second's delete removed the first's brand-new live claim, so both created and both served; each then held a guard pointing at the other's file, so the first to exit released the second's claim. Clearing is now a `rename`, which is atomic over the name: the process that moves the stale file aside is the one that unlinked it, and everybody else's rename fails with `ENOENT`. `Drop` also checks the file is still the one it wrote. | — |
| **A claim was keyed on the path's spelling** | Fixed before it shipped. `canonicalize` resolves symlinks and `..` but does not case-fold, so `~/Vault` and `~/vault` are one directory on macOS and were two claims; a `mount --bind` alias does the same with two genuinely different canonical paths. Keyed on the directory's device and inode now — what the filesystem itself calls it, so every spelling arrives at one claim. **That has a tail, and it was measured rather than reasoned about**: a filesystem reissues an inode once the directory using it is deleted, so a throwaway vault can leave a claim that a later, unrelated vault has to disprove — and disproving it cost a `fork`+`exec` of `kill`. A few hundred of them, left by the e2e suite killing its servers, pushed `start → editable` from ~450 ms to 596 ms and failed §06. So staleness is now settled by one `stat` first: a running server holds a directory it opened, so a claim naming a root that is not there cannot be one anybody is serving. The pid probe is what remains for the case that needs it. | A platform without inodes, where this falls back to the path. |
| **`register new "Rust: a survey"` silently destroyed the note's title and tags** | Fixed. The title was spliced into the frontmatter raw, and a bare `: ` inside a plain YAML scalar is a syntax error — so the *whole* block failed to parse, `entry_for` fell back to the default, and the note lost its title **and every tag** in the INDEX and the tag index. Exit code 0, path printed, nothing on screen. ` #` was quieter and no better: it opens a comment and truncated there. Titles are now emitted as quoted scalars where YAML needs it and plainly where it does not, mirrored in `app/src/core/refs.ts` so a note made in the UI and one made by the CLI are the same file. Not a §04 change — a double-quoted scalar is the same value, and the frozen v1 fixture already carries one. | — |
| **One keystroke rewrote every line ending in a CRLF note** | Fixed at the API boundary. `tests/compat.rs` asserts the server never normalises `notes/005-crlf.md`; the client was the half that did. CodeMirror has one line ending — `EditorState.lineSeparator` sets what the editor splits and inserts, but `doc.toString()` joins with `\n` regardless, which was measured rather than assumed, because setting that facet is the obvious fix and it does not work. So a two-character edit arrived in the vault's history as a whole-file diff. `getNote` normalises a uniformly-CRLF note and `putNote` puts its endings back, which keeps the app to **one coordinate system**: offsets cross that boundary in both directions — `bodyOffset`, the OUTLINE pane, `place()` — and a CRLF buffer against an LF document would need every one of them translated. Mixed endings are left exactly as found; there is no convention there to keep. | A vault where notes are edited on Windows *and* the miss matters — this covers the file, not a per-line convention. |
| **A missing image in one folder marked that filename missing in every folder** | Fixed. Misses were recorded under the raw `src` while `Misses`' own docstring said "vault path", so `notes/shot.png` failing made the perfectly good `archive/shot.png` inert — dotted, dim and unclickable directly above the widget rendering the image it claimed was absent. Both directions now resolve through the host, which is the only thing that knows which note holds the reference. | — |
| **PROPERTIES showed one `title:` and edited a different one** | Fixed by refusing. Every reader is last-wins — `fields` builds a Map — while `setField` was first-match-wins, so a note with a duplicated key showed the second value and rewrote the first: the edit destroyed a line the user was never shown and then appeared to do nothing, because the line below still decided the title. A duplicated key is now left alone, which is the position ADR-001 already takes, and `touchModified` inherits it. | A §02b state for "this field cannot be edited and here is why" — the pane currently just declines. |
| **`If-Match: ""` created a note that had been deleted** | Fixed. The comparison flattened "no file" to `""` with `unwrap_or_default()`, so an empty entity-tag *matched* a vanished note and the write became a create — the one input the doc three lines above promised was a conflict. The value was not hypothetical: a conflict on an already-deleted path reported `current: ""`, `error_response` put it on the wire as `ETag: ""`, and a client doing exactly what that header is for got a create. Matched on the `Option` now. | — |
| **A path below a regular file answered 500** | Fixed. `notes/CLAUDE.md/x.md` raises `ENOTDIR`, not `ENOENT`, so a request the server understood perfectly well was reported as the server having broken — and logged to stderr as a vault fault every time. Reachable from the UI, not only from curl: `cleanFolder` accepts `CLAUDE.md` as a folder. `NotADirectory` now reads as `NotFound`. | — |
| **A test claimed to prove the ping is not sent at connect time** | Fixed. `an_idle_socket_is_pinged` asserted the frame's *payload length* under a comment about *timing*, which is true whenever the ping fires — so deleting `pump`'s priming `tick()` left it green. It now reads the clock, and was checked against its own absence: without the prime it reports the ping firing 3 ms after the socket opened. | — |
| **The git-status cache was stale for the fetch that matters most** | Fixed by making the claim true rather than softening it. The doc said the fetch-save-fetch window "cannot happen" because the watcher clears the cache — but the watcher answers after a 50 ms coalescing window and a blocking flush, while the client refetches the tree the instant a PUT returns. Every note created from the palette read a GIT field describing the vault before the note existed. A write clears the cache itself now: a write knows it wrote. | — |
| **Four documents described a product that had changed** | Fixed: `docs/agents.md` still said `register new` must be run inside the vault (`--vault` landed a week earlier); `CONTRIBUTING.md` told contributors to install Chromium only, so the `pnpm e2e` gate it names could not pass; `scaffold.rs` said nothing reads `config.json` and Screen 6 has not been built, when both are read and built; and `deploy/Dockerfile` plus `tests/release.rs` described the published image as `scratch` and cited a test that does not exist. | — |
| **`THIRD-PARTY.md` understated what the container redistributes** | **Corrected, and this one wants your eye.** It said the image was `scratch` and "redistributes nothing the binary does not", which stopped being true when the published image became Alpine + git. The real inventory is twenty-eight packages, taken from the image rather than from memory, and ten of them are GPL-2.0-only — `git`, `busybox`, `apk-tools`, `alpine-baselayout`, `ssl_client`, `scanelf` — plus two GPL-2.0-or-later and one MPL-2.0. This does not touch REGISTER's own MIT licence: the binary runs `git` as a subprocess, and an image is aggregation rather than a combined work. What it does mean is that pulling the image obtains GPL software, and Alpine publishes the corresponding source for all of it. | ~~Closed at your instruction: the release notes carry it.~~ Alpine publishing the sources would have discharged this on its own — that is how every derived image works — but the cheap half of the argument won: a release page listing five binaries and a container tag is exactly where somebody looks and concludes there is nothing to look for. `deploy/image-notice.md` is prepended to the generated notes on every tag, and to an existing release on a re-run, since a release keeps the notes it was created with and that branch is where it would quietly go missing. Held by `every_release_says_what_the_image_redistributes`, which also fails if either image stops being the one the notice describes. |
| **~~SPEC §07 still says the container is `scratch`~~** | ~~Closed at your instruction (Rev R).~~ The listing described a runtime neither image has had since the GIT field was fixed, and it had drifted in five other ways nothing was comparing it against: pnpm restated as `pnpm@10` rather than read from `packageManager` — §06's own doctrine broken in the one file the doctrine is about — plus the missing `build.rs`, `--locked`, `USER 1000:1000`, the `safe.directory` config the bind mount needs, and `--allow-tokenless-network`, without which the container refuses the binding it exists to provide. §07 also never mentioned `deploy/Dockerfile.release` at all, so the file that produces the image people actually pull was absent from the section about distribution. Rev L's claim that the published image "stays `scratch`" is left in place and marked, rather than rewritten: the revisions are a record of what was believed when. The masthead and end-of-record strings read `REV O` under a `Rev Q` title — the same drift Rev J fixed once already — so they are now checked by `the_spec_states_one_revision_of_itself` rather than corrected a third time. | — |

## Found after v0.7.0 (Renovate had run, and the audit was rerun)

PR #14 fixed Renovate's grouping before Renovate had ever run. Once it had, its
Dependency Dashboard confirmed the groups and showed the one thing a config
cannot: what a toolchain bump looks like when it arrives. Two more came from
reading the server again with the importer in mind — it is the first thing that
puts notes nobody here wrote into a vault.

| Item | The decision, and why | What would reopen it |
|---|---|---|
| **A Rust bump arrived as two PRs, each red** | Fixed. Renovate's `rust-toolchain` manager bumped `rust-toolchain.toml` in `renovate/toolchain-pins` and its `dockerfile` manager bumped `deploy/Dockerfile` in `renovate/rust-1.x`, and `tests/release.rs` asserts the two name one minor — so each branch failed on its own and only the pair merged together was green. This is the trap the toolchain group existed to prevent, and did not, because the Dockerfile was never in it. The group now takes `node` and `rust` from the `dockerfile` manager as well; Alpine stays out, because no pin file ties the base image to anything. The test's own literal (`rust:1.97-alpine`) went at the same time — after a bump it would have compared against a line no longer in the file, true forever and measuring nothing. | — |
| **A note had no size cap, and `GET /api/tree` read every note whole** | Fixed, at the 16 MiB media has had since it was served. Every note used to arrive through the capped `PUT`, so the tree could assume a note was bounded; the importer copies whatever it is given, and an agent always could. A note over the cap is still listed — the INDEX must show a file the vault holds — with its title and tags left empty, and opening it answers 413 rather than loading it. The number lives in `vault.rs` now and the router reads it, so the two halves of the API cannot drift apart. | **Parked with it:** the walk still reads every note *under* the cap on every load. An entry cache keyed on the etag would make a repeat load a walk of stats — in-process and derived, the same standing as the git-status cache — and is the next step if a real vault measures slow. |
| **The watcher could forget every note it knew** | Fixed. `known` — the set that tells a create from a change — was moved into the flush task, and a panic there was answered with `unwrap_or_default()`: an empty set, held until restart, after which every write reported as a create and every deletion of a note it had once known was dropped unsaid. Rebuilt from the filesystem instead, the way it is built at start. No panic path in `flush` is known; this is for the one nobody has found yet. | — |

## Decided after v0.3.2 (a week of real use)

| Item | The decision, and why | What would reopen it |
|---|---|---|
| **A daily log is knowledge, and there is one of them every day** | Hidden from the INDEX, kept in every derivation. Three dated rows in a real vault raised it: `daily/` sorts before `notes/`, so a week puts seven rows *above* everything you wrote and a year puts three hundred and sixty-five. That is the P8 argument a second time — stencils among your notes bury the notes — and it closed the same way. What is new is that a daily log, unlike furniture, is knowledge: its `- [ ]` is where people actually write tasks and its `[[…]]` belongs in the graph. `isListed` and `isDerived` were nested, so "hidden and counted" could not be said at all; they are independent predicates now, and `tasks.test.ts` pins a daily log's task reaching TODAY so the nesting cannot come back quietly. Today's log stays one `G D` away and every log stays in ⌘K. | Anyone wanting to browse the journal by date. The honest answer then is not to re-list them — it is a dated surface of their own, which §02b does not draw. |

## Decided after v0.3.1 (first use in anger)

Entries here were raised by someone using the app rather than by a phase, and
answered rather than deferred. They are recorded because the alternative is
re-arguing them every time a new pair of eyes arrives.

| Item | The decision, and why | What would reopen it |
|---|---|---|
| **The index could only be reached with three Tab presses** | Fixed rather than parked. §02b gives a nav row "↑↓ / j–k traversal" and draws no way to *enter* the list, so the frame's primary navigation cost `Tab` `Tab` `Tab` — past INV and ⌘K — before a single traversal key did anything, and ⌘K was in practice the only fast route to a note. §01 promises a whole session without a mouse; that promise was being paid for with the mouse's own affordance. `j`/`↓` from `<body>` now enters at the top and `k`/`↑` at the bottom, each key keeping the direction it has inside the list. **This is a binding §02b does not draw** — the alternative was leaving a documented gap in §01's central claim, and the keys are §02b's own rather than new ones. `FOCUS · INDEX` appears in ⌘K so the binding is named on screen, per §01's "every control shows its key". | A §02b revision that draws entry into a pane, which should also settle whether the inspector's panes deserve the same. |
| **The WATCHER lamp is red when live, and red usually means broken** | Kept as it is. It is a **tally light**, not a traffic light — the red lamp on a camera, a transmit LED, a record light, where red means *on air* rather than *fault*. The slow pip reinforces it: fault lamps are steady, activity lamps pulse. Green is not available as an addition either, because §02 is "monochrome plus a **single** signal colour" and CONTRIBUTING names §02 the one permanent constraint — a second accent is a different product. The colour could be *changed* rather than added (`--signal` is one token), but it is also the editor caret, the active-line marker, the palette caret and its selected row, so `#ff2a00` is a large part of how this looks and the LED is only one of eight consumers. | More than one person reading the lamp as an alarm. One report is a convention mismatch; a pattern is a design fault, and the cheapest answer then is not green — it is that the dot is redundant beside a label already reading `Watcher Live`, and could go. |
